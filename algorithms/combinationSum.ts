function combinationSum(candidates: number[], target: number): number[][] {
  const allCombinations: number[][] = [];

  // for (const num of candidates) {

  // }
  return allCombinations;
}

// object of allowable frequencies
// when a combination is made up minus frequency from that key?

// use floored division to see how many times each number could fit into target
// recalculate when a number is used and minus one from frequency

// DFS tree to find any combination
